export interface Customer {
  id: string;
  broker_id: string;
  name: string;
  email: string;
  gstin: string;
}

export interface User {
  id: string;
  name: string;
  email: string;
  gstin: string;
}
